// TODO: try writing a "Summarization Node" that automatically moves old conversation turns from the SQLite history (the db the checkpointer uses) into your Vector Memory (the vectra db that the MCP memory server uses to remember(store) and recall(retrieve) facts)

// TODO: add more documents to knowledge base and test the rag mcp server out properly
